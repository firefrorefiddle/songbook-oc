import { PrismaClient } from "@prisma/client";
import { readFileSync, readdirSync, existsSync } from "fs";
import { join, basename } from "path";

const prisma = new PrismaClient();

const LIEDERMAPPE_DIR = "/home/mike/src/Liedermappe";
const LIEDER_DIR = join(LIEDERMAPPE_DIR, "lieder");
const TEX_DIR = join(LIEDERMAPPE_DIR, "tex");

const SONGBOOK_FILES = [
  "Gemeinde.tex",
  "Gemeinde_OH.tex",
  "Gemeinde_TO.tex",
  "Jugend.tex",
  "Jugend_OH.tex",
  "Jugend_TO.tex",
  "eigene_mappe.tex",
  "Eigene_Mappe_2.tex",
  "Eigene_Mappe_3.tex",
];
